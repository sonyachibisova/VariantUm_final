# ВариантУм — схема БД и структура проекта

## Часть 1. Схема базы данных (PostgreSQL 16)

### 1.1 ER-диаграмма (текстовая)

```
USERS (1) ──< (N) PROJECTS (1) ──< (N) VARIANTS (1) ──< (N) TASKS
   │                  │                       │
   │                  │                       │
   │                  └──< (N) PROJECT_VERSIONS (история)
   │                  │
   │                  └──< (N) RECOMMENDATIONS
   │
   ├──< (N) UPLOADED_FILES
   │
   └──< (N) LLM_REQUEST_LOG (для rate limit и аудита)
```

### 1.2 Таблицы

#### `users`
```sql
CREATE TABLE users (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email           VARCHAR(255) NOT NULL UNIQUE,
    password_hash   VARCHAR(255) NOT NULL,
    full_name       VARCHAR(255) NOT NULL,
    role            VARCHAR(50) NOT NULL DEFAULT 'TEACHER',
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    deleted_at      TIMESTAMPTZ
);
CREATE INDEX idx_users_email ON users(email) WHERE deleted_at IS NULL;
```

#### `projects` — комплекты вариантов
```sql
CREATE TABLE projects (
    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id             UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    title               VARCHAR(500) NOT NULL,
    mode                VARCHAR(50) NOT NULL, -- 'FROM_REFERENCE' | 'FROM_CRITERIA'
    subject             VARCHAR(100),
    grade               INT,
    topic               VARCHAR(500),
    task_type           VARCHAR(50),         -- 'PROBLEM' | 'TEST' | 'EXERCISE' | 'OPEN_QUESTION'
    target_difficulty   INT,                 -- 1..5
    difficulty_gradation VARCHAR(50),        -- 'EQUAL' | 'ASCENDING' | 'CUSTOM'
    custom_prompt       TEXT,
    reference_text      TEXT,                -- если mode=FROM_REFERENCE
    reference_file_id   UUID REFERENCES uploaded_files(id),
    params_json         JSONB,               -- полные параметры генерации
    status              VARCHAR(50) NOT NULL DEFAULT 'GENERATING',
                        -- 'GENERATING' | 'READY' | 'FAILED'
    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    deleted_at          TIMESTAMPTZ
);
CREATE INDEX idx_projects_user ON projects(user_id) WHERE deleted_at IS NULL;
CREATE INDEX idx_projects_search ON projects(user_id, subject, grade) WHERE deleted_at IS NULL;
CREATE INDEX idx_projects_topic_trgm ON projects USING GIN(topic gin_trgm_ops);
```

#### `variants` — варианты в комплекте
```sql
CREATE TABLE variants (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id      UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    index_in_project INT NOT NULL,
    difficulty      INT,
    total_estimated_minutes INT,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE (project_id, index_in_project)
);
CREATE INDEX idx_variants_project ON variants(project_id);
```

#### `tasks` — отдельные задания внутри варианта
```sql
CREATE TABLE tasks (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    variant_id      UUID NOT NULL REFERENCES variants(id) ON DELETE CASCADE,
    index_in_variant INT NOT NULL,
    text            TEXT NOT NULL,
    answer          TEXT,
    steps           INT,
    estimated_minutes INT,
    difficulty      INT,
    task_type       VARCHAR(50),
    metadata_json   JSONB,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE (variant_id, index_in_variant)
);
CREATE INDEX idx_tasks_variant ON tasks(variant_id);
```

#### `project_versions` — история версий комплекта
```sql
CREATE TABLE project_versions (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id      UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    action          VARCHAR(50) NOT NULL,
                    -- 'GENERATED' | 'MANUAL_EDIT' | 'AI_EDIT_SINGLE'
                    -- 'AI_EDIT_ALL' | 'REGENERATED' | 'VARIANT_ADDED' | 'VARIANT_DELETED'
    description     VARCHAR(500),
    snapshot_json   JSONB NOT NULL,  -- полный снимок проекта для отката
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_versions_project ON project_versions(project_id, created_at DESC);
```

#### `recommendations` — рекомендации по комплекту
```sql
CREATE TABLE recommendations (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id      UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    type            VARCHAR(50) NOT NULL,
                    -- 'DIFFICULTY_OUTLIER' | 'DUPLICATE_ANSWER'
                    -- 'TIME_MISMATCH' | 'ADD_VARIATION' | 'CUSTOM'
    variant_index   INT,
    severity        VARCHAR(20),    -- 'INFO' | 'WARNING' | 'CRITICAL'
    message         TEXT NOT NULL,
    dismissed       BOOLEAN NOT NULL DEFAULT FALSE,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_recs_project ON recommendations(project_id) WHERE dismissed = FALSE;
```

#### `uploaded_files` — загруженные эталоны
```sql
CREATE TABLE uploaded_files (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    filename        VARCHAR(500) NOT NULL,
    mime_type       VARCHAR(100) NOT NULL,
    size_bytes      BIGINT NOT NULL,
    storage_key     VARCHAR(500) NOT NULL, -- путь в MinIO: user_{id}/files/{uuid}
    parsed_text     TEXT,
    parser_used     VARCHAR(50),
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_files_user ON uploaded_files(user_id);
```

#### `llm_request_log` — журнал запросов к LLM (rate limit + аудит)
```sql
CREATE TABLE llm_request_log (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    project_id      UUID REFERENCES projects(id) ON DELETE SET NULL,
    prompt_type     VARCHAR(50) NOT NULL,
                    -- 'ANALYZE' | 'GENERATE_REF' | 'GENERATE_CRIT'
                    -- 'VALIDATE' | 'AI_EDIT_SINGLE' | 'AI_EDIT_ALL' | 'RECOMMEND' | 'TIME_ESTIMATE'
    tokens_used     INT,
    duration_ms     INT,
    success         BOOLEAN NOT NULL,
    error_message   TEXT,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_llm_log_user_time ON llm_request_log(user_id, created_at DESC);
```

#### `form_assignments` — задания для учеников (онлайн-формы)
```sql
CREATE TABLE form_assignments (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id      UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    user_id         UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    mode            VARCHAR(20) NOT NULL, -- 'CLASS_LIST' | 'INDIVIDUAL_LINKS'
    access_token    VARCHAR(64) UNIQUE,   -- CLASS_LIST: единственная ссылка на всё задание
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

#### `form_students` — ученики в режиме CLASS_LIST
```sql
CREATE TABLE form_students (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    assignment_id   UUID NOT NULL REFERENCES form_assignments(id) ON DELETE CASCADE,
    full_name       VARCHAR(255) NOT NULL,
    variant_id      UUID NOT NULL REFERENCES variants(id),
    access_token    VARCHAR(64) NOT NULL UNIQUE,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

#### `form_variant_tokens` — токены вариантов в режиме INDIVIDUAL_LINKS
```sql
CREATE TABLE form_variant_tokens (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    assignment_id   UUID NOT NULL REFERENCES form_assignments(id) ON DELETE CASCADE,
    variant_id      UUID NOT NULL REFERENCES variants(id),
    access_token    VARCHAR(64) NOT NULL UNIQUE,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

#### `student_submissions` — сданные работы учеников
```sql
CREATE TABLE student_submissions (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    assignment_id   UUID NOT NULL REFERENCES form_assignments(id) ON DELETE CASCADE,
    variant_id      UUID NOT NULL REFERENCES variants(id),
    student_name    VARCHAR(255) NOT NULL,
    answers_json    JSONB NOT NULL,   -- [{"taskId":"uuid","answer":"..."}]
    auto_score      JSONB,            -- [{"taskId":"uuid","correct":true/false/null}]
    teacher_review  JSONB,            -- [{"taskId":"uuid","comment":"...","grade":"5"}]
    submitted_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 1.3 Row-Level Security (RLS)

```sql
-- Включаем RLS для всех таблиц с пользовательскими данными
ALTER TABLE projects ENABLE ROW LEVEL SECURITY;
ALTER TABLE uploaded_files ENABLE ROW LEVEL SECURITY;

-- Политика: пользователь видит только свои проекты
CREATE POLICY projects_user_isolation ON projects
    USING (user_id = current_setting('app.current_user_id')::UUID);

-- Аналогично для других таблиц через JOIN'ы или прямые user_id
```

В Spring при каждом запросе ставим `SET app.current_user_id = '...'` через JDBC-перехватчик.

---

## Часть 2. Структура проекта (Monorepo)

```
variantum/
├── README.md                      ← описание проекта, как запустить
├── docker-compose.yml             ← postgres + redis + minio + backend + frontend
├── .env.example                   ← пример переменных окружения
├── .gitignore
│
├── docs/                          ← вся проектная документация
│   ├── 01_Анализ_проекта.md
│   ├── 02_Карточка_прототипа.md
│   ├── 03_Требования_и_критерии.md
│   ├── 04_Техническое_задание.md
│   ├── 05_API_спецификация.md
│   ├── 06_Схема_БД_и_структура.md
│   └── 07_Промпты_для_GigaChat.md
│
├── backend/                       ← Java Spring Boot
│   ├── pom.xml                    ← Maven, зависимости
│   ├── Dockerfile
│   ├── mvnw, mvnw.cmd
│   └── src/
│       ├── main/
│       │   ├── java/ru/variantum/
│       │   │   ├── VariantumApplication.java
│       │   │   ├── config/
│       │   │   │   ├── SecurityConfig.java       ← JWT, CORS, фильтры
│       │   │   │   ├── GigaChatConfig.java        ← WebClient для GigaChat
│       │   │   │   ├── MinioConfig.java
│       │   │   │   ├── RedisConfig.java
│       │   │   │   ├── RateLimitConfig.java       ← Bucket4j
│       │   │   │   └── OpenApiConfig.java         ← Swagger
│       │   │   ├── domain/                        ← JPA entities
│       │   │   │   ├── User.java
│       │   │   │   ├── Project.java
│       │   │   │   ├── Variant.java
│       │   │   │   ├── Task.java
│       │   │   │   ├── ProjectVersion.java
│       │   │   │   ├── Recommendation.java
│       │   │   │   ├── UploadedFile.java
│       │   │   │   ├── LlmRequestLog.java
│       │   │   │   ├── FormAssignment.java         ← онлайн-форма (задание для учеников)
│       │   │   │   ├── FormStudent.java            ← ученик в режиме CLASS_LIST
│       │   │   │   ├── FormVariantToken.java       ← токен варианта в режиме INDIVIDUAL_LINKS
│       │   │   │   └── StudentSubmission.java      ← сданная работа ученика
│       │   │   ├── repository/                    ← Spring Data JPA
│       │   │   │   ├── UserRepository.java
│       │   │   │   ├── ProjectRepository.java
│       │   │   │   └── ... (по одному на entity)
│       │   │   ├── service/                       ← бизнес-логика
│       │   │   │   ├── auth/
│       │   │   │   │   ├── AuthService.java
│       │   │   │   │   └── JwtService.java
│       │   │   │   ├── parser/                    ← парсинг файлов
│       │   │   │   │   ├── FileParser.java       ← интерфейс
│       │   │   │   │   ├── PdfBoxParser.java
│       │   │   │   │   ├── DocxParser.java
│       │   │   │   │   ├── TxtParser.java
│       │   │   │   │   └── TesseractOcrParser.java
│       │   │   │   ├── llm/                       ← интеграция GigaChat
│       │   │   │   │   ├── GigaChatClient.java    ← OAuth, completion, vision; авто-фолбэк 402→Lite
│       │   │   │   │   ├── PromptBuilder.java     ← загрузка шаблонов из classpath; build(name, vars)
│       │   │   │   │   ├── LlmJsonUtil.java       ← extractJson + sanitizeJsonEscapes (LaTeX-починка)
│       │   │   │   │   ├── GeneratedModels.java   ← GeneratedSet / GeneratedVariant / GeneratedTask(figure)
│       │   │   │   │   ├── AnalyzeService.java
│       │   │   │   │   ├── TextCleanupService.java
│       │   │   │   │   ├── GenerateFromReferenceService.java
│       │   │   │   │   ├── GenerateFromCriteriaService.java
│       │   │   │   │   ├── AiEditSingleService.java
│       │   │   │   │   ├── AiEditAllService.java
│       │   │   │   │   ├── ValidatorService.java
│       │   │   │   │   ├── RecommenderService.java
│       │   │   │   │   └── TimeEstimatorService.java
│       │   │   │   ├── project/
│       │   │   │   │   ├── ProjectService.java
│       │   │   │   │   ├── VariantService.java
│       │   │   │   │   ├── VersionService.java
│       │   │   │   │   ├── GenerationOrchestrator.java
│       │   │   │   │   └── FormService.java           ← создание форм, автопроверка, ответы
│       │   │   │   ├── export/
│       │   │   │   │   ├── ExportService.java
│       │   │   │   │   ├── FigureSvgRenderer.java ← inline SVG для PDF: 6 типов фигур включая geometry
│       │   │   │   │   ├── PdfExporter.java       ← iText 7
│       │   │   │   │   └── DocxExporter.java      ← docx4j
│       │   │   │   ├── storage/
│       │   │   │   │   └── MinioStorageService.java
│       │   │   │   └── ratelimit/
│       │   │   │       └── RateLimitService.java
│       │   │   ├── controller/                    ← REST endpoints
│       │   │   │   ├── AuthController.java
│       │   │   │   ├── FileController.java
│       │   │   │   ├── AnalyzeController.java
│       │   │   │   ├── ProjectController.java
│       │   │   │   ├── VariantController.java
│       │   │   │   ├── ExportController.java
│       │   │   │   └── HealthController.java
│       │   │   ├── dto/                           ← DTO для REST
│       │   │   │   ├── request/
│       │   │   │   └── response/
│       │   │   ├── mapper/                        ← MapStruct мапперы
│       │   │   └── exception/                     ← кастомные исключения
│       │   │       ├── ResourceNotFoundException.java
│       │   │       ├── RateLimitException.java
│       │   │       └── GlobalExceptionHandler.java
│       │   └── resources/
│       │       ├── application.yml                ← основная конфигурация
│       │       ├── application-dev.yml
│       │       ├── application-prod.yml
│       │       ├── db/migration/                  ← Flyway-миграции
│       │       │   ├── V1__init_schema.sql
│       │       │   ├── V2__add_recommendations.sql
│       │       │   ├── V3__add_photo_url_to_tasks.sql
│       │       │   └── V4__add_online_forms.sql    ← form_assignments, form_students, student_submissions
│       │       ├── prompts/                       ← текстовые шаблоны промптов
│       │       │   ├── analyze.txt
│       │       │   ├── generate_from_reference.txt
│       │       │   ├── generate_from_criteria.txt
│       │       │   ├── ai_edit_single.txt
│       │       │   ├── ai_edit_all.txt
│       │       │   ├── validate.txt
│       │       │   ├── recommend.txt
│       │       │   └── estimate_time.txt
│       │       └── pdf-templates/
│       │           └── variant_template.html      ← для iText
│       └── test/
│           └── java/ru/variantum/
│               ├── integration/                   ← интеграционные тесты
│               └── unit/
│
├── frontend/                      ← React + Vite + TypeScript
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── tailwind.config.js
│   ├── Dockerfile
│   ├── nginx.conf                 ← для прод-сборки
│   ├── index.html
│   └── src/
│       ├── main.tsx
│       ├── App.tsx
│       ├── routes.tsx
│       ├── api/                   ← клиенты бэкенда
│       │   ├── client.ts          ← axios/fetch с интерсепторами
│       │   ├── auth.api.ts
│       │   ├── files.api.ts
│       │   ├── projects.api.ts
│       │   ├── exports.api.ts
│       │   ├── forms.api.ts       ← создание форм-заданий (приватные endpoints)
│       │   └── submissions.api.ts ← публичные /form/* + приватные /submissions/*
│       ├── store/                 ← Zustand stores
│       │   ├── authStore.ts
│       │   ├── projectStore.ts
│       │   └── uiStore.ts
│       ├── pages/
│       │   ├── LoginPage.tsx
│       │   ├── RegisterPage.tsx
│       │   ├── DashboardPage.tsx
│       │   ├── ModeSelectionPage.tsx
│       │   ├── FromReferencePage.tsx    ← Режим A
│       │   ├── FromCriteriaPage.tsx     ← Режим B
│       │   ├── ProjectEditorPage.tsx    ← основной экран работы
│       │   ├── LibraryPage.tsx
│       │   ├── SettingsPage.tsx
│       │   ├── HelpPage.tsx
│       │   ├── StudentFormPage.tsx       ← /form/:token — публичная страница ученика
│       │   └── StudentSubmissionsPage.tsx ← /projects/:id/submissions — ответы учеников
│       ├── features/              ← фичи как самостоятельные блоки
│       │   ├── auth/
│       │   ├── upload/
│       │   │   ├── FileDropzone.tsx
│       │   │   ├── OcrPreview.tsx
│       │   │   └── TextInput.tsx
│       │   ├── analysis/
│       │   │   └── AnalysisForm.tsx
│       │   ├── criteria/
│       │   │   └── CriteriaForm.tsx
│       │   ├── generator/
│       │   │   ├── GenerationParams.tsx
│       │   │   ├── DifficultyGradationPicker.tsx
│       │   │   ├── CustomPromptField.tsx
│       │   │   └── StreamProgress.tsx
│       │   ├── variants/
│       │   │   ├── VariantsTable.tsx
│       │   │   ├── VariantCard.tsx
│       │   │   ├── DiffHighlighter.tsx
│       │   │   └── RecommendationsBanner.tsx
│       │   ├── editor/
│       │   │   ├── WysiwygEditor.tsx        ← Tiptap
│       │   │   ├── AiEditSingleDialog.tsx
│       │   │   └── AiEditAllDialog.tsx
│       │   ├── history/
│       │   │   └── VersionHistoryPanel.tsx
│       │   ├── exporter/
│       │   │   ├── ExportDialog.tsx
│       │   │   ├── FieldsPicker.tsx
│       │   │   ├── LayoutPicker.tsx
│       │   │   └── FormExportDialog.tsx  ← диалог создания онлайн-формы
│       │   ├── library/
│       │   │   └── ProjectsList.tsx
│       │   └── onboarding/
│       │       ├── OnboardingTour.tsx       ← react-joyride
│       │       └── VideoGuideModal.tsx
│       ├── components/            ← переиспользуемые UI-компоненты
│       │   ├── FigureSvg.tsx      ← SVG-рендер 6 типов фигур (geometry, triangle, circle, …)
│       │   ├── MathText.tsx       ← KaTeX-рендер LaTeX-формул
│       │   ├── RichText.tsx       ← markdown + MathText + FigureSvg
│       │   ├── ui/                ← shadcn/ui-стиль
│       │   │   ├── Button.tsx
│       │   │   ├── Input.tsx
│       │   │   ├── Dialog.tsx
│       │   │   ├── Tooltip.tsx
│       │   │   ├── Slider.tsx
│       │   │   ├── Select.tsx
│       │   │   ├── Tabs.tsx
│       │   │   └── ...
│       │   ├── layout/
│       │   │   ├── Header.tsx
│       │   │   ├── Sidebar.tsx
│       │   │   └── PageContainer.tsx
│       │   └── feedback/
│       │       ├── Toast.tsx
│       │       └── Spinner.tsx
│       ├── hooks/
│       │   ├── useAuth.ts
│       │   ├── useGenerationStream.ts     ← SSE-подключение
│       │   └── useDebounce.ts
│       ├── lib/                   ← утилиты
│       │   ├── format.ts
│       │   └── katex.ts
│       ├── styles/
│       │   └── globals.css
│       └── types/                 ← TypeScript типы (общие с бэком)
│           ├── api.ts
│           ├── project.ts
│           └── ...
│
├── infra/                         ← инфраструктура и деплой
│   ├── docker/
│   │   └── nginx.conf
│   ├── scripts/
│   │   ├── start-dev.sh
│   │   └── seed-demo-data.sh
│   └── deploy/
│       └── deploy.sh
│
└── .github/
    └── workflows/                 ← CI/CD (опционально)
        └── ci.yml
```

---

## Часть 3. Локальный запуск (быстрый старт)

### Требования
- Docker и docker-compose
- JDK 21
- Node.js 20+
- Аккаунт GigaChat (для API-ключа)

### Команды

```bash
# 1. Клонировать репозиторий
git clone https://github.com/variantum-team/variantum.git
cd variantum

# 2. Скопировать .env
cp .env.example .env
# отредактировать .env: GIGACHAT_API_KEY, DB_PASSWORD и т.д.

# 3. Поднять инфраструктуру
docker-compose up -d postgres redis minio

# 4. Запустить миграции и бэкенд
cd backend
./mvnw spring-boot:run

# В отдельном терминале — фронтенд:
cd frontend
npm install
npm run dev

# Открыть http://localhost:5173
```

### Production-сборка

```bash
docker-compose up --build -d
```

Доступ:
- Frontend: http://localhost (через Nginx)
- Backend API: http://localhost/api
- Swagger: http://localhost/api/swagger-ui.html
- MinIO console: http://localhost:9001

---

## Часть 4. Переменные окружения

```bash
# .env.example

# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=variantum
DB_USER=variantum
DB_PASSWORD=changeme

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# MinIO
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=changeme
MINIO_BUCKET=variantum-files

# GigaChat
GIGACHAT_API_URL=https://gigachat.devices.sberbank.ru/api/v1
GIGACHAT_AUTH_URL=https://ngw.devices.sberbank.ru:9443/api/v2/oauth
GIGACHAT_CLIENT_ID=<UUID из GigaChat Studio>
GIGACHAT_CLIENT_SECRET=<UUID-секрет, НЕ base64-строка>
GIGACHAT_SCOPE=GIGACHAT_API_PERS
GIGACHAT_MODEL=GigaChat-2-Pro         # основная (Pro)
GIGACHAT_MODEL_LITE=GigaChat          # лёгкая бесплатная; "GigaChat-2-Lite" не существует
GIGACHAT_MODEL_MAX=GigaChat-2-Pro     # тяжёлая (опц.)

# JWT
JWT_SECRET=<сгенерированная_длинная_строка>
JWT_ACCESS_TOKEN_TTL=604800     # 7 дней
JWT_REFRESH_TOKEN_TTL=2592000   # 30 дней

# Rate limit
RATE_LIMIT_LLM_PER_HOUR=30

# App
APP_BASE_URL=http://localhost:8080
FRONTEND_BASE_URL=http://localhost:5173
```
